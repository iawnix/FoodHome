# FoodHome App

Flutter client for the FoodHome V0.0 local prototype.

Current scope:

- In-memory household state.
- Tonight order flow.
- Kitchen status transitions.
- Family menu maintenance.
- Taste preference and exclusion settings.

Firebase configuration is intentionally absent. Add generated Firebase files
only in an environment-specific setup and keep them out of version control.
